# Run Ledger — all install attempts

Chronological debugging record. Each run: what happened, root cause, fix
(commit). Full evidence in the session; distilled here for handoff.

| Run | Outcome | Root cause(s) found | Fix |
|-----|---------|---------------------|-----|
| 1 | Bootstrap got BGP static pods + correct frr-peers.json (pipeline proof) but kube-vip printed CLI usage and exited; render-config-frr panicked | kube-vip manifests lacked the `manager` subcommand; runtimecfg cannot derive cluster domain from the bootstrap kubeconfig | MCO fd072b1a0: `args: [manager]` ×3 manifests; `--cluster-config /opt/openshift/manifests/cluster-config.yaml` + volume (mirrors keepalived) |
| 2 | Cluster came up with **keepalived** | `make redeploy` regenerates install-config → bgpVIPConfig patch lost | Procedural: never use `make redeploy`; use `ocp_cleanup && build_installer install_config && re-patch && ocp_run` |
| 3 | First BGP session established (bootstrap↔ToR) but `(Policy)` — no routes exchanged; kube-vip: "in-cluster config" error | FRR 9/RFC 8212 requires policy on eBGP (ToR side); kube-vip needs an explicit kubeconfig | ToR `no bgp ebgp-requires-policy` (cb1adfc); MCO c1bf266e1 `k8s_config_file` env |
| 4 | kube-vip still "in-cluster config" error despite env | **Upstream kube-vip ignores `K8sConfigFile`** in the manager path (hardcodes admin.conf/~/.kube/in-cluster) | kube-vip 173173011: manager honors explicit kubeconfig (+`kubernetes_addr` override); MCO 5b311c8ee: point at `https://localhost:6443` (avoids VIP-dependency deadlock at pivot) |
| 5 | Surgical image swap on live bootstrap → **first VIP advertisement + API reachable via BGP route**; masters joined. Then: `vipManagement` empty on live Infra; kube-vip backend loop erroring | (a) stock payload's Infrastructure CRD prunes `vipManagement` + gate unknown; (b) `backend.Entry.Check()` ALSO hardcodes kubeconfig paths | (a) build `cluster-config-api` from our api branch, add to payload; (b) kube-vip 8cd17f786 backend honors configured kubeconfig |
| 6 | Gates + vipManagement persist ✓; masters ran BOTH frr-k8s DaemonSet AND stuck static pods; CNO's FRRConfiguration denied by webhook | (a) DaemonSet scheduled before label-node could label (and NodeRestriction denies the label anyway); (b) frr-k8s webhook requires router-level `prefixes` for toAdvertise | CNO a62d892c0: router prefixes + role-based DaemonSet anti-affinity under BGP; MCO bd240b8c6: drop label-node |
| 7 | All-master sessions from static config (`?` origin) ✓; controller/frr-status crashloop: cache sync timeouts | RBAC bound to `system:nodes`, but `/etc/kubernetes/kubeconfig` identity = **SA openshift-machine-config-operator/node-bootstrapper** (verified `oc auth whoami`); also missing `frrk8sconfigurations` | CNO 07a24912c (subject + resource), then 0a457915 (secrets/pods namespace reads). Also c04ab0abb: status manager treated desired==0 DaemonSet as hung (blocked co/network on compact clusters) |
| 8 | **CRD handover happened** — but advertisement flipped to origin `i` (unconditional network statements); masters with mid-rollout apiservers kept advertising → ECMP into dead backends → install self-strangled | CRD `prefixes`/`toAdvertise` render as static `network` statements — health gating destroyed | CNO 9222d8407: CR carries sessions only; advertisement via `redistribute table-direct 198` + route-map/prefix-list filters in rawConfig (bootstrap-identical semantics) |
| 9 | Sessions ✓, VIP health-gated INTO bgpd's table ✓ (weight 32768 `?`) but 0 prefixes sent; frr-status crash (Secret cache) | frr-k8s renders deny-all per-neighbor outbound route-maps when toAdvertise absent | CNO ecb5282fe: `toAdvertise.allowed.mode: all` (later superseded, see run12); RBAC secrets fix |
| 10 | bgpd table EMPTY on masters; zebra not tracking table 198; manual import-table/route-replace probes failed | Suspected `ip import-table` missing from CR config (bootstrap config has it) | CNO 93e48830: `ip import-table 198` in rawConfig. Probes inconclusive → built the standalone FRR lab |
| lab | **Isolated the real bug** in a container against the exact payload image: routes pre-existing in table 198 at config time are never redistributed (zebra import clears SELECTED on source routes; even de-selects previously selected ones); any later netlink add heals; `ip route replace` does NOT | **Upstream FRR bug**, fixed in 10.7 by FRRouting/frr b2c17ad52 | Backported onto 10.4.3 (`patches/frr/0001-zebra-Do-not-clear-selected-flag-on-route-about-to-b.patch`), built patched zebra (CentOS Stream 9, glibc 2.34 ABI), overlay image `metallb-frr:bgp-demo`, added to payload. Lab matrix fully green after (reload path needs +5s route-map timer) |
| 11 | (control run, killed early — superseded by lab evidence) | — | — |
| 12 | FRR patch verified in-cluster (VIP selected + in bgpd table post-handover) but PfxSnt=0; frr-status: "resource name may not be empty" | frr-k8s `mode: all` = "all DECLARED router prefixes" (source-verified `prefixesToAdvertiseForFamily`) → renders `deny any` allowed-lists; CRD cannot express advertising redistributed routes. frr-status needs `--pod-name` | CNO be5b5ae9a: drop toAdvertise; rawConfig appends high-seq permits to frr-k8s's generated `<peer>-out` route-maps (prefix-list deny = no-match = fall-through; leak-proof). MCO d6b2df182: `--pod-name=frr-k8s-$(NODE_NAME)` |
| 13 | **API VIP advertised from all 3 masters post-handover, gated (`?` origin)**; pods 4/4; ingress VIP absent while routers Running | kube-vip-ingress gated on `:29445/healthz` = the API haproxy monitor (503 forever); the router health is `:1936/healthz` (keepalived's chk_ingress) — EP doc error | MCO 29e4f8042: gate on `http://localhost:1936/healthz` |
| 14 | **ALL CRITERIA MET.** Both VIPs advertised, ingress only from router-bearing masters; 35/36 COs Available; console HTTP 200 over BGP-routed VIP; FRRNodeState + BGPSessionState(Established×3) | Sole failure: karpenter — "failed to initialize cloud provider: unsupported platform" on baremetal; base-nightly bug, identical without BGP | none needed |
| — | (aborted install, worker scope-out) BMHs stuck "registering"; Redfish GETs failing | sushy-tools wedged — "client socket is closed" on every request | `podman restart sushy-tools` on the hypervisor; also: stale `ironic_nodes.json` (3 nodes) after raising NUM_WORKERS fails install_config → `rm` it + `make configure` |
| 15 | First run with workers (NUM_MASTERS=3 NUM_WORKERS=2): routers on workers, 5/5 sessions Established, but ingress VIP ABSENT from ToR; console 200 was the in-subnet L2 fallback masking it | Worker kube-vip + kernel table-198 route + DaemonSet FRR config (import-table/table-direct/route-maps) all correct; zebra's table-198 view EMPTY. Manual `ip route replace` healed instantly → kube-vip's one-shot route add loses a race | kube-vip 6d51cbd: RT mode re-asserts the VIP route (netlink RouteReplace) every healthy check cycle. (First version 8b92df2 had a dead branch — `routeMgr.Add` returns fs.ErrExist steady-state so the `else if err == nil` re-assert never ran; amended) |
| 16 | STILL asymmetric (1 worker + 2 masters advertising) despite level-triggered re-assertion | `ip monitor` forensics: kube-vip re-asserts, but the kernel emits NO netlink event for a no-op replace; run15's "manual heals" only worked because iproute2's default scope (link) differed from kube-vip's (global) = a real change. Deeper: .56/.60 masters' zebra learned the route only via later health-flap del/add churn — ALL 5 nodes missed the ORIGINAL event | → lab TEST E |
| lab | **TEST E** (`lab/frr-lab-addr-route-race.sh`) reproduced in the metallb-frr:bgp-demo container: same-prefix interface address (kube-vip AddIP, deprecated /32) + table-198 route arriving in the same netlink batch → zebra keeps the kernel route but never imports/redistributes it (2/5 with `ip -batch`; addr→route separately = OK, route→addr = 100% broken) | **NEW zebra bug**, distinct from the SELECTED-flag one (RHEL-193997); the patched 10.4.3 zebra does NOT cover it | kube-vip 7d27248: alternate the (inert) route realm attribute 1↔2 on each re-assertion so every replace is a real kernel change and emits a netlink event. Workaround, not a fix — zebra bug filed upstream: FRRouting/frr#22654 |
| 17 | **ALL CRITERIA MET on the full cluster, zero manual intervention.** Ingress .4 ECMP from exactly the two router workers (.23/.24), API .5 ECMP from 3 masters, single cluster-wide `bgp-vip` CR, console 200, `ip route get` = L3 path. Failover: cordon worker-0 + delete router pod → path withdrawn ~50s (health threshold), console 200 throughout via worker-1; uncordon → path restored ≤20s | Note: a plain pod delete does NOT withdraw — the replacement router is Ready again before the failure threshold | none needed |
| lab | **Root cause of FRRouting/frr#22654 found**: zebra's `connected_remove_kernel_for_connected()` (zebra/connected.c) calls `rib_meta_queue_early_route_cleanup()` (zebra/zebra_rib.c) which removed QUEUED same-prefix kernel routes matching prefix+type only — NO table comparison — destroying the pending table-198 route when the VIP address's connected route arrives in the same netlink batch. Upstream master already scopes by afi/safi/vrf_id (and fixed a use-after-free in the debug path that 10.4.3 still has) but STILL lacks the table check — bug alive upstream. Pitfall found during validation: must overlay BOTH `/usr/lib/frr/zebra` AND `/usr/libexec/frr/zebra` — first lab run only mounted /usr/lib and falsely showed the fix not working | ~10-line fix: add `uint32_t table_id` param, compare `ere->re->table == table_id`, caller passes `zvrf->table_id`; also moved the entry free below the debug log that dereferenced it (UAF) | frr 8989c33 "zebra: scope early route queue cleanup to the matching table" (branch `table-scoped-early-cleanup` of mkowalski/frr, on frr-10.4.3 + f06eb02 SELECTED-flag backport; `patches/frr/0002-*`). Lab: frr-lab-addr-route-race.sh 10/10 green (E2 route→addr was 100% broken), batched E5 5/5 (was 2/5), frr-lab.sh matrix A–D unchanged (C1 +5s route-map timer quirk = documented baseline). metallb-frr:bgp-demo overlay rebuilt with both patches |
| 18 | **ALL CRITERIA MET with zero manual intervention — THE FRR FIX ALONE IS ENOUGH.** Clean install (3 masters + 2 workers): patched zebra + PRE-WORKAROUND kube-vip (image 8c533d1, byte-identical to run15's which failed on all 5 nodes) → ingress .4 ECMP from both router workers, API .5 from 3 masters, no realm attribute in kernel routes (workaround provably absent), console 200 | Proves the kube-vip realm-toggle workaround (7d27248) is NOT required when running the patched zebra — kept only as optional defense-in-depth (openshift/kube-vip#6) | kube-vip:bgp-demo tag re-pointed at pre-workaround 8cd17f7 build; metallb-frr:bgp-demo carries both zebra patches |
| 19 | Reverted the demo env to the workaround configuration: zebra rebuilt at f06eb02 (SELECTED-flag patch only, table-scope fix NOT deployed), kube-vip back on the realm-toggle build (7d27248). Clean install: all criteria met, realm 2 visible in table 198 on workers (workaround provably active) | Deliberate: run the pre-FRR-fix stack while the table-scope fix (mkowalski/frr 8989c33) awaits upstream review on FRRouting/frr#22654 | Demo env = kube-vip workaround stack; run18 proved the FRR fix alone suffices when it lands |
| 20 | Dropped `ip import-table 198` from CNO rawConfig (55c0da0fb; folded into #3047 first commit 865d35827). Clean install, workaround stack: rawConfig and node running-config carry no import-table, zebra table-198 view populated, NO table-route in main RIB (distance-15 pollution gone), full advertisement (ingress from 2 workers, API from 3 masters), console 200 | Lab + FRR 10.5 docs: `redistribute table-direct` reads the kernel table directly; the run5-era "zebra needs import-table" belief was an artifact of debugging before the deny-any and zebra bugs were isolated. MCO never carried it — CNO-only change | NEXT-STEPS D2 resolved; frr-k8s design PR #470 updated accordingly (table range 1-65535, no import-table emission) |
| proto | **Metrics companion prototype VALIDATED** (live on the run20 cluster, master-0): static pod frr-sockets/frr-conf switched emptyDir→hostPath (/run/frr-k8s/*, hot-edited manifest); companion DaemonSet (frr-metrics + serving-cert TLS, labels joining the existing frr-k8s-monitor-service) reads the static FRR via hostPath and Prometheus scrapes it (up=1 at .56:9141, frrk8s_bgp_announced_prefixes_total=1 = API VIP) | Static pods cannot mount Secrets, so the metrics exporter could never live in-pod; observability does not need static pod survivability, so it moves to a masters-only companion DS with normal SA/certs | Productization notes: dedicated Service/SM for the companion (shared Service probes dead :9140 on companion pods); /run/frr-k8s perms need tmpfiles.d or a root init (prototype used chmod 0777); MCO template change = 2 volumes; kubelet noticed manifest edits only after touch+stopp; frr-status move (RBAC shrink) not yet exercised |
| 21 | **Metrics companion productized and validated on a clean install**: MCO unit prepares /run/frr-k8s (0777, emptyDir parity) + static pod hostPath volumes; CNO renders the masters-only companion DS with dedicated Service/ServiceMonitor. frrk8s_bgp_announced_prefixes_total in Prometheus from ALL 5 nodes (3 masters via companion, 2 workers via DS), zero dead targets, VIPs/console green | First attempt crash-looped: 0770 dirs break watchfrr (runs as root WITHOUT CAP_DAC_OVERRIDE - not owner, not group = EACCES); and runAsUser 100 on the companion breaks vtysh ("failed to connect to any daemons" - needs root, like the worker DS's image-default). SELinux was NOT involved (spc_t both sides; chcon dropped) | CNO a099392f2 (dev), MCO f1784cd7e (dev; amended). D8 RESOLVED for real. Fold into #3047 + MCO PR next |
| 22 | **Hardened metrics companion validated on a clean install**: hostNetwork dropped (pod-network scrape), caps drop ALL (+DAC_OVERRIDE), readOnlyRootFilesystem, seccomp, tcpSocket probes; all 7 frr targets up, 5-node coverage, VIPs/console green | Hot-validation caught TWO regressions the hardening introduced: (1) losing hostNetwork loses spc_t - the companion became MCS-confined container_t and could not touch var_run_t hostPath content, so the boot unit needs `chcon container_file_t` after all (run21's "chcon unnecessary" only held for the spc_t static pod); (2) the namespace default-deny NetworkPolicy (Ingress+Egress) never applied to hostNetwork pods - the pod-network companion needs an explicit policy: ingress 9141 from openshift-monitoring, egress 443/6443 for TokenReview (apiserver endpoints are node IPs, unselectable). tcpSocket probes cause cosmetic TLS-EOF log spam | CNO d9a13552b (dev) / #3047 c4 747a690e2; MCO 1a13a1ea5 (dev). Declined from review: CPU limits (requests-only convention), runAsNonRoot (vtysh needs root), SA token unmount (TokenReview) |
| 23 | First run on a CURRENT nightly base (5.0.0-0.nightly-2026-07-19-060131, k8s 1.36): sessions came up from static config, then the frr-k8s static pods' running-config went EMPTY and the API VIP advertisement vanished (only bootstrap advertising); network CO Degraded; install timed out | The 1.36 apiserver rejects the frr-k8s FRRConfiguration CRD's `asn` fields (`format: int32` + `maximum: 4294967295` is unsatisfiable) → CNO could not apply `bgp-vip` → the static pod controller synced the EMPTY CR set and wiped the boot config. **Upstream CNO bug** (present on master) | DEMO-CARRY on CNO dev branch: int32→int64 for both asn fields in bindata 001-crd.yaml. Upstream fix = #3070 (OCPBUGS-99074; our duplicate #3080 closed). Deeper handover lesson: CNO-degraded ⇒ no CR ⇒ static pods withdraw — the CRD-handover design couples VIP survival to CNO health |
| 24 | **ALL CRITERIA MET on the 07-19 nightly** (after three unrelated-to-BGP fights): API .5 ECMP ×3 masters, ingress .4 ×2 workers, console 200, `frrk8s_bgp_session_up`=5 | (a) compatibility-requirements webhook (NEW DevPreview-gated component from cluster-capi-operator, `failurePolicy: Fail` on ALL CRD writes) timed out for ~2h → every operator applying CRDs Degraded. Root-caused LATER (see docs/compatibility-webhook-investigation.md): pods created during install convergence were network-black-holed; pod recreation fixed it; the "contentType=" log line was a red herring (live experiments: apiserver sends correct application/json). (b) worker BMHs never registered until a BMO restart. (c) workers stuck in firstboot + crio sandbox pulls: nightly art-dev images unsigned but rendered policy.json enforces sigstore (BOTH ocp-v4.0-art-dev AND ocp-v5.0-art-dev scopes — pause image is v5.0) | (a) CVO override + scale operator&controllers to 0 + delete both VWCs (order matters; lighter alternative: just delete the controller pods); (b) `oc rollout restart deploy/metal3-baremetal-operator`; (c) patch /etc/containers/policy.json (insecureAcceptAnything for both art-dev scopes) on workers + restart crio — TWICE (firstboot rebase reboots into a fresh /etc) |
| 25 | Post-rebase run (all 6 branches rebased onto upstream mains, base bumped to 07-20-081439): masters up fast, but frr-k8s static pods AND the CNO DaemonSet both ran on masters → 127.0.0.1:7572 bind collision crashloops; network CO stuck | `vipManagement` EMPTY on Infrastructure status: the api-repo rebase left GENERATED payload CRD manifests stale (post-rebase regen died at the protobuf step) — the field landed in the wrong featureset variants (present in SelfManagedHA-**TechPreview**, MISSING in SelfManagedHA-**DevPreview** = ours) → apiserver pruned the installer-set field → CNO rendered the DS without the master anti-affinity | Proper regen (`hack/update-codegen-crds.sh` + `update-payload-crds.sh` + `update-payload-featuregates.sh`), amend, rebuild cluster-config-api image **and the payload** (CRD manifests are extracted from component images at `oc adm release new` time — repushing the component tag is NOT enough). Also hit: rebased installer rejects dev-scripts' deprecated `dnsVIP` field (strip it in the install-config patch); rebase vendoring gotcha: api master moved to k8s-1.36 libs while installer/MCO/CNO mains are on 0.35 — vendor from a pinned pre-1.36 api snapshot (installer: `openshift-api-pre136`; MCO: main's pinned commit ef71f942e + cherry-picks, since its client-go needs InternalReleaseImage) |
| 26 | **ALL CRITERIA MET on the refreshed stack** (rebased branches + 07-20-081439 base): vipManagement=BGP, DS master-anti-affinity active, zero port collisions, API .5 ×3 + ingress .4 ×2 ECMP, console 200, sessions 5/5 in Prometheus. run24's webhook and sigstore workarounds NOT needed (webhook: consistent with the transient-networking root cause; sigstore: 07-20 workers pulled clean) | (a) worker BMHs stalled again until a BMO restart (2nd occurrence — file it); (b) NEW: metal3 pod is missing the static-ip-set/static-ip-manager containers → nobody assigns the `provisioningIP` (192.168.111.8, provisioningNetwork=Disabled) → ironic waits on the IP forever, image-cache init containers crashloop, zero worker provisioning | (a) BMO rollout restart; (b) manual `ip addr add 192.168.111.8/24 dev br-ex` on the metal3 node — **NON-PERSISTENT** (lost on reboot/pod move); investigate CBO rendering conditions vs our Provisioning CR before filing |
| 27 | **One-click knob + CI verify script validated end to end** (2026-07-31): cluster deployed with `BGP_VIP_MANAGEMENT=true` (dev-scripts#1939) — no manual install-config patching — and the `baremetalds-e2e-bgp-vip-verify` step from release#82698 run verbatim passes all 5 checks (vipManagement=BGP, static pods/no keepalived, ToR paths per VIP, console 200) | Two env failures on the way, neither knob-related: (a) first two attempts died at cb-bootstrap "No masters found" — **sushy-tools was wedged again** (ledger row "—"): ironic reported masters provisioned while the run17-era VMs (9 days uptime!) were never reimaged, so the OLD cluster's masters kept advertising the VIP and ECMP-poisoned the new bootstrap's API. Detection: kubelet journal dates + `virsh list` uptimes; fix: `podman restart sushy-tools` + `virsh destroy` the stale domains. (b) the run26 worker-stall (blank BMH state + missing provisioningIP .8) recurred — same two workarounds (manual `ip addr add`, BMO rollout restart). Also reconfirmed: the metal-u15 custom payload defeats the installer version parse, tripping the ≤4.4 dnsVIP guard (strip locally; CI unaffected) | Knob emits config byte-equivalent to 20+ validated runs; RUNBOOK deploy cycle simplified accordingly. The ECMP-poisoning failure mode is itself EP-relevant: a stale cluster on the same subnet advertising the same VIPs silently hijacks a new install — worth an EP note on ASN/VIP hygiene for redeploys |
| ci-1 | **First combined CI run of the whole stack** (2026-08-04, multi-PR testing: `/testwith openshift/installer/main/e2e-metal-ipi-bgp-vip` + MCO#6326 + CNO#3047 + runtimecfg#395 on installer#10718): four PRs merged into one payload, dev-scripts knob + guardrails executed in CI, installer rendered bgpVIPConfig, MCO selected the BGP path — and failed at the designed D1 fail-fast: `kube-vip ("") image is missing from the release payload` | Two blockers surfaced: (a) first attempt died in 1m — CNO#3047 no longer merged onto master (upstream added bootstrapResult/TLS plumbing to renderAdditionalRoutingCapabilities; rebased, signatures merged); (b) the ocp/5.0 integration stream never received the kube-vip istag despite the successful 2026-07-29 promotion (quay push confirmed in the postsubmit log) — escalated to DPTP on release#81957; MCO's image-references commit withdrawn again until the tag exists | Green-lane replay once the tag lands: cherry-pick the image-references commit back onto #6326, rerun the same /testwith |
| coex-1 | **OVN-K route-advertisements coexistence PROVEN on the run26 cluster** (2026-08-04, dry run of the release#82912 lane on this host): ran the adapted `baremetalds-e2e-ovn-bgp-pre` (external FRR RR at 192.168.111.3, iBGP 64512; agnhost on macvlan; `routeAdvertisements: Enabled`; receive-filtered FRRConfiguration + RouteAdvertisements CR), then BOTH verify steps — all green. **The key unknown passed: masters' static frr-k8s pods merged the ovnk-generated FRRConfigurations** (5/5 nodes Established at the RR, each advertising its pod subnet), VIP acceptance unaffected (API ×3 + ingress ×2 still at the ToR, console 200), pod→agnhost datapath over the BGP-imported route works. No collision between the host-net ToR (.1:179) and the bridge-net RR (.3:179) | One lane bug found and fixed (release#82912, 0edc8226b98): `RouteAdvertisements` apply raced CNO's CRD creation — on BGP-VIP clusters the frr-k8s DS pre-exists so the script's rollout waits return immediately (existing ovn-bgp lanes are masked by the DS-creation wait); added explicit CRD wait to the shared pre step. Also confirmed the namespace-idempotency fix is required (stock script would have died at `oc create namespace`) | Cluster left with RA enabled + RR running (steady-state coexistence soak; `podman exec frr vtysh -c 'show bgp summary'`). Revert recipe: delete RouteAdvertisements/receive-filtered CRs, patch routeAdvertisements out, `podman rm -f frr agnhost`, remove host routes/iptables for 10.128.0.0/14 |
| coex-2 | **Day-2 MetalLB-BGP coexistence PROVEN on the run26 cluster** (2026-08-05, dry run of the `baremetalds-e2e-bgp-vip-metallb` lane): installed the MetalLB operator day-2 via upstream raw manifests (no `metallb-operator` packagemanifest in the catalog → manifest fallback, SHA-pinned `194f9719` from main — v0.14 lacks FRRK8sExternalMode), MetalLB CR `bgpBackend: frr-k8s-external` pointing at `openshift-frr-k8s` (NO second frr-k8s deployed; speaker runs `--frrk8s-namespace=openshift-frr-k8s`), pool `bgp-vip-lane-pool` .30-.50 + BGPPeer `tor` (192.168.111.1/64513, myASN 64512) + `bgp-vip-lane-adv`, workload `lb-echo` got LB IP 192.168.111.30 in <5s. ToR sessions 5→5 (same-neighbor merge, delta 0); LB /32 at ToR with Paths: (5 available), converged ~15-20s (CI must poll); datapath green (`ip route get` via ToR path, curl :8080/hostname); VIP + OVN-K RA verifies still green → **THREE FRRConfiguration producers coexisting** on the shared frr-k8s (bgp-vip + ovnk-generated-* + metallb-*) | Findings for the lane scripts: (a) raw-manifest install creates TWO deployments (controller-manager + webhook-server) — wait on both; (b) webhook-server pod rejected by restricted-v2 SCC (SA `controller`, uid 65534) → `oc adm policy add-scc-to-user nonroot-v2 -n metallb-system -z controller`, retry backoff ~2-3 min absorbed by 5m rollout timeout; (c) **deadlock on OpenShift**: controller/speaker mount `*-certs-secret` but the serving-cert-annotated Services only render with `DEPLOY_SERVICEMONITORS=true` (`oc set env` the operator; upstream bug candidate); (d) MetalLB CR creation must gate on webhook-server rollout + a `--dry-run=server` probe (both validatingwebhookconfigurations reject until serving); (e) frrk8s.metallb.io CRDs pre-exist (openshift-frr-k8s) → apply says "configured" not "created", not an error; (f) metallb FRRConfigurations (`metallb-<node>` ×5) carry **no labels** — select by name prefix, `-l app=metallb` returns nothing. Lane code staged locally on `bgp-vip-ovn-bgp-lane` (commits 1fd7252, 088971d, 7263363); push to release#82912 pending user approval | Cluster end-state: MetalLB operator + CR + lb-echo left running (coexistence soak alongside coex-1's RA/RR). Revert: `oc delete -n metallb-system metallb/metallb bgpadvertisement/bgp-vip-lane-adv bgppeer/tor ipaddresspool/bgp-vip-lane-pool`; `oc delete deploy/lb-echo svc/lb-echo`; `oc delete -f` the SHA-pinned metallb-operator.yaml manifest (SCC rolebinding goes with the namespace) |
| 28 | **Dual-stack dry run (gap D4) — v4 GREEN, three v6 findings** (2026-08-05, IP_STACK=v4v6 + local knob extension rendering a second ToR peer over v6). The **entire config plumbing is dual-stack complete with zero code changes**: installer knob → bgp-vip-config (v4+v6 peers AND VIPs) → frr-peers.json → FRRConfiguration/rendered FRR (ipv6 AF with `redistribute table-direct 198 route-map BGP-VIP-ROUTES-V6`, v6 neighbor activated, /128 prefix-lists, raw egress permits). v4 acceptance verify passes VERBATIM on the dualstack cluster; v6 sessions Established 5/5 once the ToR listened on v6 | **Finding 1 (env) — CLOSED (2026-08-05)**: the ToR container survived `make clean` with stale single-stack config because this working copy predated merged #1929's 02_configure_host hook (knob carried as a hand-applied diff). Fixed properly: /root/dev-scripts moved to upstream master 4559bc6 (hooks present: every `make` re-renders the ToR via 02_configure_host, `host_cleanup.sh` removes it — the stale scenario cannot happen on a current checkout, no upstream change needed); sole remaining local delta = the v6-peer knob extension (follow-up PR candidate). Re-render validated: both listen ranges, 5 v4 + 5 v6 sessions re-established, VIP routes intact. **Finding 2 (product, kube-vip/MCO) — FIXED (2026-08-06)**: kube-vip manifests carried a single `address` = VIPs[0]; v6 VIPs unmanaged. Fixed: MCO renders per-family instances (0011-kube-vip-api-secondary masters / 0021-kube-vip-ingress-secondary all nodes, disabled-manifests gate on len(VIPs)>1; bootstrap manifest + getPlatformManifests dualStackVIPs param) — commit 5d24d8366 pushed to MCO#6326 with tests. EN ROUTE found the settled-cluster restart gap (D13) LIVE: the secondary instance (started day-2) health-checks Node addresses → TLS fail (apiserver certs cover neither node IPs nor [::1]; ONLY the v4 loopback listener serves a 127.0.0.1-SAN cert). Fixed in kube-vip: backend health check honors the configured kubernetes_addr override (branch backend-kubernetes-addr, 83f03d4 — upstream **PR OPEN: kube-vip/kube-vip#1671**) + MCO manifests switch kubernetes_addr to https://127.0.0.1:6443 (localhost may Happy-Eyeballs to ::1). PROVEN LIVE end to end: secondary pod (test image v6-backend-test) writes fd2e::5/128 into table 198 AND the ToR receives the /128 via MP-BGP — the full v6 chain works. **Finding 3 — RETRACTED (test artifact, 2026-08-06)**: the original injection used proto 196; re-testing with proto 248 (what kube-vip uses) zebra imports the v6 route immediately and the ToR receives it. ipv6 table-direct works. Residual curiosity (not product-relevant): zebra ignores proto-196 routes in watched tables — 196 is likely within FRR's own protocol range; no action | Deploy waits on the karpenter CO (same platform bug as run26; cluster healthy). Findings 1-3 all closed (1 env-fixed, 2 code-fixed + pushed, 3 retracted). master-0 still runs the hand-placed secondary manifest with the v6-backend-test image (soak); remove or replace when the payload kube-vip is rebuilt. Remaining follow-ups: upstream kube-vip PR for the backend override; dev-scripts knob v6-peer PR; full dualstack install with rebuilt MCO+kube-vip images (run29 candidate); EP updated (dualstack instances + loopback endpoint) |
| hot | **MCO PR #6326 review hardening validated live on the run26 cluster** (hot-edited master-0 static pod manifests, watched pod + ToR through each change): (1) `drop: ALL` on the frr container — even with the 4 network caps + DAC_OVERRIDE re-added — kills FRR: first watchfrr vty-socket EACCES, then "all configured daemons failed to start" (privilege-separated startup needs root-implicit SETUID/SETGID/CHOWN); reverted to upstream metallb parity, documented in-manifest. (2) reloader/frr-status DO tolerate `drop: ALL` but need `add: [DAC_OVERRIDE]` (root vtysh vs frr-owned sockets — same class as the run21 companion lesson). (3) resource limits added everywhere, sized from `oc adm top --containers`: frr-status idles at ~390m CPU (busy-poll suspect, possible upstream frr-k8s issue), so long-running small containers got 500m ceilings, not the naive 100m. (4) `terminationGracePeriodSeconds` 0→10: SIGKILL still closes TCP (plain peers withdraw fine) but a graceful-restart HELPER peer retains stale VIP routes on bare TCP loss; SIGTERM'd bgpd sends Cease NOTIFICATION = hard reset, defeats GR retention. (5) review found a real bug: VIP-COMMUNITY route-map was defined but never attached to any neighbor | Review-driven hardening needed live verification — two of the five suggestions were wrong as stated and would have broken the VIP path | All folded into the #6326 series (kube-vip pods additionally: drop ALL + NET_ADMIN/NET_RAW, no-escalation, RO rootfs; readOnly input mounts; `set -eu`; IsBGPVIPManagement requires ingress VIPs; bootstrap rejects empty config.json). Declined with recorded rationale: probes on frr/kube-vip, runAsNonRoot (securityContext caps are not *effective* for non-root without file caps on the binary), bootstrap hostPath (kubelet creates emptyDir 0777; the day-2 boot unit replicates exactly that) |


## Scope extension (2026-07-10..13): ingress VIP on a full cluster

Runs 15–17 validated routers on WORKERS (NUM_MASTERS=3 NUM_WORKERS=2,
WORKER_VCPU=8 WORKER_MEMORY=16384 WORKER_DISK=60). Enabling code changes:

- MCO 47948106d (`OPNET-595-bgp-vip-management-dev`): moved
  `templates/master/00-master/on-prem/files/0020-kube-vip-ingress.yaml` →
  `templates/common/` (keepalived parity: ingress pod on ALL nodes incl.
  arbiter, health-gated inert without a local router). kube-vip-api stays
  master-only.
- CNO d65941092 (dev branch, folded into PR #3047's first commit —
  force-pushed, still 3 commits): FRRConfiguration CR renamed
  `bgp-vip-master` → `bgp-vip`, nodeSelector REMOVED (cluster-wide; masters'
  static pod + workers' DaemonSet consume the same sessions + rawConfig).
- kube-vip 6d51cbd + 7d27248 (`OPNET-595-bgp-vip-management`, pushed): RT
  mode re-asserts the VIP route via netlink RouteReplace every healthy check
  cycle, alternating the inert realm attribute 1↔2 so each re-assertion is a
  real kernel change (kernel emits NO netlink event for a no-op replace).

## Meta-lessons for whoever continues

1. **The render pipeline was where all the bugs were** — hacking around it
   (hardcoded images, manual manifests) would have proven nothing. Testing
   through the real installer/MCO/CNO path surfaced 14 distinct product bugs.
2. **In-subnet VIPs mask BGP failures** (kube-vip also puts the address on the
   interface → L2 ARP serves it). Several runs "half-worked" because of this.
   For strict L3 validation, move the VIPs off-subnet (future work).
3. **`make redeploy` is a trap** (run2). The redeploy recipe is in RUNBOOK.md.
4. The exact-image standalone lab (lab/) cracked in 30 minutes what
   cluster-level probing couldn't in hours — build the small repro early.
5. **Overlay BOTH zebra paths.** The metallb-frr image ships zebra at
   `/usr/lib/frr/zebra` AND `/usr/libexec/frr/zebra`; overlaying only one
   silently runs the unpatched binary (cost a false "fix doesn't work" lab
   result before run18).
6. **Live-validate review-suggested hardening before pushing it.** Of the
   MCO PR #6326 review round, two suggestions would have silently broken the
   VIP path (`drop: ALL` on the frr container; probes that restart-flap all
   BGP sessions on a node) and one was based on a wrong premise (emptyDir
   UID-writability — kubelet creates them 0777). Hot-editing the static pod
   manifest on one master and watching the ToR is a five-minute test that
   settles any of these definitively.
